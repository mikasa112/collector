-- 模块注册总纲
--
-- 只有在这里列出的脚本文件名（相对本目录顶层，不含子目录）才会被引擎加载执行，
-- 未列出的 .lua 脚本即使存在于目录下也不会运行。
-- 本文件本身以 "_" 开头，不会被当作模块加载。
--
-- 允许写任意 Lua 逻辑（比如按条件判断），只需最终 return 一个字符串数组即可，
-- 引擎在启动时以及每次本文件被修改时都会重新执行取值。
return {
    "main.lua",
}
